# 环境配置说明

## 快速开始

### 1. 设置API密钥

#### 方法一：使用 .env 文件（推荐用于开发环境）

```bash
# 进入 FinanceAI.Service 目录
cd src/FinanceAI.Service

# 复制环境变量模板
copy .env.example .env   # Windows
# 或
cp .env.example .env     # Linux/Mac

# 编辑 .env 文件，填入您的真实API密钥
# SILICONFLOW_API_KEY=sk-xxxxxxxxxxxxxxxxxx
```

#### 方法二：设置系统环境变量（推荐用于生产环境）

**Windows (CMD):**
```cmd
set SILICONFLOW_API_KEY=your_api_key_here
```

**Windows (PowerShell):**
```powershell
$env:SILICONFLOW_API_KEY="your_api_key_here"
```

**Linux/Mac:**
```bash
export SILICONFLOW_API_KEY=your_api_key_here
```

### 2. 获取API密钥

1. 访问 [SiliconFlow](https://siliconflow.cn/)
2. 注册/登录账号
3. 进入控制台获取API密钥

### 3. 验证配置

```bash
# 进入服务目录
cd src/FinanceAI.Service

# 启动服务
python runserver.py
```

如果配置正确，您应该看到：
```
✅ 配置加载成功. API密钥: sk-pr****hlel
```

如果配置错误，您会看到：
```
❌ SILICONFLOW_API_KEY is required!
请通过以下方式之一设置:
1. 创建 .env 文件: SILICONFLOW_API_KEY=your_key
2. 设置环境变量: set SILICONFLOW_API_KEY=your_key (Windows)
```

## 部署说明

### Docker 部署

在 `docker-compose.yml` 中设置环境变量：

```yaml
version: '3.8'
services:
  finance-ai:
    build: ./src/FinanceAI.Service
    environment:
      - SILICONFLOW_API_KEY=${SILICONFLOW_API_KEY}
    ports:
      - "8000:8000"
```

启动时传入环境变量：
```bash
SILICONFLOW_API_KEY=your_key docker-compose up
```

### 云服务部署

在云平台（如阿里云、腾讯云、AWS等）的环境变量配置中添加：

- **变量名**: `SILICONFLOW_API_KEY`
- **变量值**: `你的API密钥`

## 安全注意事项

⚠️ **重要**：
- ✅ `.env.example` 可以提交到 Git（不包含真实密钥）
- ❌ `.env` 文件**绝对不要**提交到 Git（包含真实密钥）
- ❌ **不要**在代码中硬编码API密钥
- ✅ 已在 `.gitignore` 中添加 `.env` 保护

## 配置项说明

| 配置项 | 说明 | 默认值 | 是否必填 |
|--------|------|--------|----------|
| `SILICONFLOW_API_KEY` | SiliconFlow API密钥 | - | ✅ 必填 |
| `APP_NAME` | 应用名称 | AcountAI | 可选 |
| `DEBUG` | 调试模式 | true | 可选 |
| `HOST` | 服务监听地址 | 0.0.0.0 | 可选 |
| `PORT` | 服务端口 | 8000 | 可选 |

## 常见问题

### Q: 提示找不到 SILICONFLOW_API_KEY？

**A:** 确保：
1. `.env` 文件在 `src/FinanceAI.Service` 目录下
2. 文件内容格式正确：`SILICONFLOW_API_KEY=your_key`（注意没有引号）
3. 或者已设置系统环境变量

### Q: Windows 环境变量设置后不生效？

**A:** 
1. 使用 PowerShell 时，重启终端使环境变量生效
2. 或在当前会话中设置：`$env:SILICONFLOW_API_KEY="your_key"`

### Q: 如何验证环境变量是否设置成功？

**A:**
```bash
# Windows (CMD)
echo %SILICONFLOW_API_KEY%

# Windows (PowerShell)
echo $env:SILICONFLOW_API_KEY

# Linux/Mac
echo $SILICONFLOW_API_KEY
```

## 开发团队注意

在团队协作时：
1. 每个开发者需要自己创建 `.env` 文件
2. 不要分享 `.env` 文件中的真实API密钥
3. 使用团队共享的开发环境密钥（如果有）
4. 生产环境使用独立的API密钥
